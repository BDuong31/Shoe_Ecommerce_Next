import WishlistView from "@/sections/wishlist/view/wishlist-view";

export const metadata = {
    title: 'Wishlist | Baso Spark',
}

export default function AddressPage() {
    return <WishlistView />;
}